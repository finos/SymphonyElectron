/**
 * Comparing the size of a single file then print it out to console
 *
 * @param current @typeDef number - size as bytes
 * @param byte @typeDef number - size as bytes
 * @param byte @typeDef number - size as bytes
 */
export declare function compareAndPrint(
  currentSize: number,
  previousSize: number,
  threshHoldSize: number,
): void;

declare const _default: {
  compareAndPrint: typeof compareAndPrint;
};

export default _default;
